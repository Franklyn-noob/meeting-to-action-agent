# Core agent + runtime
strands-agents
strands-agents-tools   # built-in tools: current_time, http_request, shell, sleep
bedrock-agentcore

# Model providers
httpx
openai
boto3

# Backend API
fastapi
uvicorn[standard]
pydantic

# Tests
pytest
